// Re-export from engine constants (single source of truth)
export {
  ITEM_HEIGHT,
  MEMORY_SCROLL_DURATION_MS as SCROLL_DURATION_MS,
  MEMORY_SCROLL_SPEED_PX_PER_FRAME as SCROLL_SPEED_PX_PER_FRAME,
  MEMORY_SETTLE_FRAMES as SETTLE_FRAMES,
} from "../../engine/constants.js";
