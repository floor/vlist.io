vlist — full changelog
initial commit → latest
639 commits · 89 days · Feb 2 – May 1, 2026


2026-05-01  v1.7.3

  fix(sortable): prevent neighbor item blink on drop
    Add --settling CSS class to suppress background-color/opacity
    transitions during sort finalize. When setItems() reorders data,
    the renderer toggles selection/focus classes on reordered items —
    the CSS transition was causing a visible blink on neighbor items.

  fix(sortable): smooth drop transition for drag source item
    Replace inline opacity:0 with a CSS class (vlist-item--drag-source)
    for the dragged item's original element. The --settling class
    suppresses the opacity transition on drop, preventing the fade-in
    blink when the ghost returns to the original position.

  fix(sortable): animate ghost back to origin on Escape cancel
    Pressing Escape during a pointer drag now animates the ghost back
    to the original position before emitting sort:cancel, matching the
    behavior of dropping outside the viewport.

  fix(selection): seed selection state before first render on snapshot restore
    Add _seedSelection internal method to populate selection state
    synchronously before the first render, eliminating the selection
    blink when recreating a list with snapshot restore.


2026-05-01  v1.7.2

  fix(scrollbar): show scrollbar immediately when autoHide is false (#43)
    updateBounds() always left the scrollbar hidden on creation/rebuild,
    requiring a user interaction to reveal it. Now calls show() when
    autoHide is disabled.

  feat(sortable): add ghostContainer config option
    Allows specifying a custom parent element for the drag ghost so it
    inherits scoped CSS (theme variables, list-specific item styles).

  fix(sortable): fix cursor, escape cancel, and drop-at-same-position (#42)
    - Keep grabbing cursor during drag via CSS and document.body.style
    - Escape key cancels an active pointer drag
    - Emit sort:cancel when item is dropped back at original position
    - Emit sort:cancel on pointercancel

  fix(build): preserve spaces around + and - in CSS minifier
    calc() expressions need spaces around operators to be valid.

  refactor(scrollbar): rename clickBehavior 'page' to 'scroll'
    'scroll' better describes the behavior from the user's perspective.
    'page' is kept as a backwards-compatible alias, normalized at runtime.


2026-04-30  v1.7.1

  fix(sortable): fix drop index oscillation on direction change
    The old computeDropIndex switched between ghost bottom/top edge based
    on a direction flag, causing a discontinuity of draggedItemSize when
    the drag reversed — triggering double-shifts or oscillation near the
    reversal point. Replaced with a stateless, direction-agnostic approach
    that scans from dragIndex using the natural leading edge for each
    direction: bottom edge for downward checks, top edge for upward checks.

  perf(sortable): use O(log n) binary search in computeDropIndex
    Replace linear scan from dragIndex with sizeCache.indexAtOffset()
    binary search. Avoids iterating through all items between dragIndex
    and the ghost position after auto-scrolling large distances.

  fix(sortable): refactor drop index calculation and element recycling
    - Replace DOM-walking computeDropIndex with sizeCache.indexAtOffset()
      to avoid wrong results from element recycling shuffling DOM order
    - Add afterRenderBatch handler to maintain drag visual state when
      virtual scrolling recycles elements (hide dragIndex, re-apply shifts)
    - Simplify cleanupDrag to reset ALL children's opacity/pointerEvents
      instead of only the tracked draggedElement
    - Add withScale to conflicts list

  fix(sortable): clear text selection after drop (Safari)


2026-04-28  v1.6.5

  feat(snapshots): save and restore keyboard focus across navigation
    getScrollSnapshot() now captures focusedId (the currently focused item ID)
    alongside scroll position and selection. restoreScroll() restores it after
    data loads at the restored position — for both sync lists (via rAF) and
    async lists (after loadVisibleRange resolves).
    - Focus ring is not shown immediately on restore — it appears only when
      the user tabs into the list, landing directly on the saved item
    - autoSave now also saves on focus:change so arrow-key navigation is
      persisted on page reload, not just scroll and selection
    - New focus:change event emitted when keyboard focus moves to a new item
    - New _getFocusedId() internal method bypasses the focusVisible gate so
      focusedId is captured correctly even after clicking away from the list
    Closes #29.

  fix(snapshots): restoreScroll clips last items when withScale is active
    maxScroll was computed as virtualSize − containerSize, ignoring the
    compression slack that withScale adds. For 1M-item lists this was ~466 px
    short — enough to restore to item 999,962 instead of the true last items.
    A second bug overwrote the correct content height (set by withScale's
    enhancedUpdateCompressionMode) with the slack-less virtualSize immediately
    after updateCompressionMode(), undoing the correct value.
    Both issues are fixed: maxScroll now reads viewportState.totalSize (which
    includes slack), and content size is only updated for non-compressed lists.
    Closes #30.

  fix(table): custom scrollbar invisible/doubled in table mode
    scrollbar-width: none (set to hide the native scrollbar) applies to both
    axes in Firefox, making the horizontal scrollbar disappear too. Native
    horizontal scrolling via trackpad and keyboard arrow keys still worked but
    the scrollbar thumb was invisible. Fixed by scoping the hide to the
    vertical axis only when horizontal overflow is present.


2026-04-27  v1.6.4

  feat(scrollbar): per-side padding support
    padding now accepts a number (all sides) or { top?, right?, bottom?, left? }
    for independent control per edge. Omitted sides default to 2px.
    - New ScrollbarPadding type exported from public API
    - Replaces --vlist-custom-scrollbar-padding with four per-side CSS variables:
      --vlist-custom-scrollbar-padding-{top,right,bottom,left}
    - Thumb travel uses scroll-axis sides (top+bottom / left+right)
    - Hover zone default uses wall-side padding (right for vertical, bottom for horizontal)
    - Stable gutter reserves right-padding + width + left-padding per axis

  feat(scrollbar): add padding, clickBehavior, minThumbSize, and hoverZoneWidth options
    - padding: inset the track from viewport edges (default: 2px)
    - clickBehavior: 'page' (default) scrolls by one page toward the click with
      smooth continuous scroll on hold; 'jump' centers the thumb instantly
    - minThumbSize: minimum thumb length in pixels (default: 15); also settable
      via --vlist-custom-scrollbar-min-thumb-size CSS variable
    - hoverZoneWidth defaults to wall-side padding + 16px so the hover zone
      always covers the full inset track area regardless of padding

  fix(scrollbar): extend click target into padding margin
    The edge zone element is now always created (not only when showOnHover is
    true) so clicks anywhere in the padding margin trigger the same page/jump
    behavior as clicking the track itself. Existing position clamping handles
    out-of-track coordinates: above the track scrolls back, below scrolls forward.

  fix(scrollbar): gutter correctly reserves padding on both sides
    Stable gutter now reserves right-padding + width + left-padding (vertical)
    or bottom-padding + height + top-padding (horizontal), ensuring the track
    never overlaps content even when asymmetric padding is configured.
    CSS variable scope fixed: padding variables are set on the viewport element
    so the gutter calc() can read them (CSS custom properties only cascade down).


2026-04-26  v1.6.3

  feat(a11y): add focusOnClick option (#32)
    Show the focus ring on mouse click for file-manager / spreadsheet UIs.
    Defaults to false (standard :focus-visible convention — click selects
    but does not show the focus ring).
    - BuilderConfig.focusOnClick for baseline single-select lists
    - SelectionFeatureConfig.focusOnClick for withSelection
    - Applies to regular clicks and Shift+clicks in multiple mode

  feat(scrollbar): add gutter option to withScrollbar
    withScrollbar({ gutter: true }) reserves stable layout space for the
    custom scrollbar track so it never overlaps content — equivalent to
    CSS scrollbar-gutter: stable. Default: false (overlay behavior).
    Implemented via vlist-viewport--gutter on the viewport element, which
    pads the scrollable content box by the track width.

  fix(page): remove stale targetScroll — use domScroll directly
    targetScroll tracked an intended scroll position across keydown events
    to handle rapid key repeat. All scrollTo calls use behavior:instant
    which updates window.scrollY synchronously — no in-flight state exists.
    The stale variable persisted across manual scroll + click interactions,
    causing _scrollItemIntoView to compute coordinates against the wrong
    position and jump the list to the wrong location.

  fix(core): auto-size horizontal root height on Windows
    On platforms with persistent scrollbars, the native horizontal scrollbar
    reduces clientHeight and clips item content. When item.height is set and
    the scrollbar is visible, vlist now measures the rendered scrollbar height
    (offsetHeight − clientHeight after a brief overflow-x: scroll probe) and
    sizes the root to item.height + scrollbarHeight so the bar sits below the
    content. Returns 0 on macOS overlay scrollbars. Skipped for withGrid,
    withMasonry, and scroll.scrollbar: 'none'.

  fix(scrollbar): gutter class moved to viewport element
    vlist-viewport--gutter replaces the former vlist--scrollbar-gutter on
    the root, consistent with vlist-viewport--custom-scrollbar and
    vlist-viewport--no-scrollbar. Padding is applied to the viewport so the
    scrollable content area correctly excludes the track width.

  style(scrollbar): split and rename CSS token namespaces
    Native and custom overlay scrollbar tokens are now separate:
    - Native:  --vlist-scrollbar-*        (thumb-color, track-color, width, radius…)
    - Custom:  --vlist-custom-scrollbar-* (thumb-color, track-color, width, radius…)
    Native scrollbar is wired to both scrollbar-width/scrollbar-color (standard,
    Chrome 121+ / Firefox 128+) and ::-webkit-scrollbar (WebKit fallback).
    Removed scrollbar-width: thin — Chrome 121+ treats keyword values as size
    presets that override ::-webkit-scrollbar { width }, making the width
    variable ineffective.

  style(scrollbar): auto-radius for custom scrollbar thumb
    --vlist-custom-scrollbar-radius defaults to calc(width / 2) so the thumb
    stays perfectly pill-shaped as the track width changes. Override the
    variable to use a fixed radius.


2026-04-24  v1.6.2

  feat(async): forward response cursor to AdapterParams on sequential reads
    Track cursorValidForOffset alongside the stored cursor. The cursor is
    forwarded to AdapterParams only when chunk.start matches exactly —
    ensuring a server cursor for offset N is never sent to a different offset.
    Cleared on reload, reset, and clear.

  fix(a11y): prevent erratic scroll on click in window mode (#23)
    Use preventScroll: true on dom.root.focus() to stop the browser's
    native scroll-into-view from fighting with window scroll mode.

  feat(page): add scrollPadding option to withPage
    Accepts { top, bottom, left, right } as numbers or () => number.
    Adjusts scrollToIndex alignment (start/center/end) to land items
    inside the safe area, clearing fixed/sticky chrome. Registers
    _scrollItemIntoView for keyboard focus navigation with the same
    inset-aware logic. Allows negative scroll positions down to -startPad
    so top items can clear the header, and extends maxScroll by endPad so
    bottom items can clear the footer.

  fix(page): use behavior:'instant' on all window.scrollTo calls
    Overrides CSS scroll-behavior:smooth that may be set on the page,
    which caused window.scrollTo to animate instead of jumping — making
    keyboard focus scroll-into-view lag behind during rapid key repeat.
    Also cleans up _scrollItemIntoView to use absolute document coordinates
    with targetScroll tracking to avoid stale DOM reads during rapid
    key repeat.


2026-04-20  v1.6.1

  feat(async): per-chunk AbortController with keepBuffer cancellation
    Each in-flight chunk request now has its own AbortController. When the
    user scrolls to a new position, any chunk more than chunkSize * 2 rows
    from the new viewport is aborted immediately, keeping concurrent requests
    to ≤ 3 (current + 1 on each side) — well under the browser's 6-connection
    HTTP/1.1 limit. Previously, stale requests were only removed from the
    internal tracking map but the underlying HTTP connection stayed open,
    causing connection pool saturation during fast scrolls over large datasets.
    AbortErrors are silently dropped and never surfaced as user-visible errors.

  feat(async): expose signal: AbortSignal in AdapterParams
    Adapters now receive an AbortSignal tied to the chunk's lifecycle.
    Pass it directly to fetch() to cancel the underlying TCP request when
    the chunk is no longer needed — not just remove it from vlist's map.
    See the updated async docs for the recommended adapter pattern.

  feat(grid/table): compression-aware visible range calculation
    withGrid and withTable now call ctx.getVisibleRange() instead of
    reading the size cache directly. This delegates to the compression-aware
    range function installed by withScale, so virtual scroll positions are
    correctly mapped to row/item indices under scroll compression.
    Previously, grid and table features bypassed the compressed range
    function and produced wrong visible ranges when withScale was active.

  perf(grid/table): fast path for unchanged range in compressed mode
    When the render range is unchanged and the list is in compressed mode,
    skip the full render (item diffing, template calls, selection checks)
    and call updatePositions() only — repositioning already-rendered
    elements is much cheaper per frame during smooth scrolling.


2026-04-16  v1.6.0

  feat: export ItemConfig, GridConfig, MasonryConfig, GroupsConfig,
  GroupHeaderConfig types from public API
    Users building item or layout configs in helper functions outside
    the inline vlist() call needed these types but couldn't import them.
    All types referenced in VListConfig are now importable from "vlist".

  fix(core): unify wheel handler, add boundary passthrough and sync
  rendering for horizontal mode
    Horizontal lists now use the same wheel-handling logic as vertical
    lists — boundary passthrough lets the parent scroll when the list
    reaches its scroll limits, and rendering is synchronous on wheel
    events to eliminate visual lag.

  feat(adapters): re-export core types from framework adapters
    vlist-react, vlist-vue, vlist-solidjs, and vlist-svelte now
    re-export VListItem, VListEvents, VList, VListConfig, ItemConfig,
    ItemTemplate, EventHandler, and Unsubscribe — users no longer need
    "vlist" as a direct dependency just for types.


2026-04-15  v1.5.6

  perf(memory): eliminate scroll-path heap growth at 1M items
    Cap content element at 16M px when compression is not active — the
    browser already clamps scrollHeight internally, but declaring 48M px
    forced Chrome to allocate internal layout/compositor structures
    proportional to the element size (~6 MB at 1M items).
    - 1M items scroll delta: 6.14 MB → 0.19 MB (97% reduction)
    - 10K items scroll delta: 0.02 MB → ~0 MB (net GC reclaim)

  perf(memory): reuse event payloads on the scroll hot path
    Pre-allocate shared objects for scroll, velocity:change, and
    range:change events — mutated in-place before each emit.
    Eliminates ~3 object allocations per scroll frame (180/sec at 60 fps).

  perf(memory): reduce per-frame allocations in render loop
    - Eliminate newElements array — add to rendered Map directly
    - Skip newlyRendered array + per-item wrapper objects when no
      afterRenderBatch hooks exist (vanilla lists without withAutoSize)
    - Guard claimPlaceholderSelection behind $.dm check — skip string
      allocations when no async data manager is active

  perf(dev): cap duplicate ID check at 10K items
    The dev-mode setItems() duplicate ID scan created a Set of ALL items.
    At 1M items this added ~70 ms to every setItems() call.  Now capped
    at the first 10K items — still catches bugs, zero cost at scale.

2026-04-15  v1.5.5

  chore: rename package from @floor/vlist to vlist
    All imports, docs, and framework adapters updated.
    @floor/vlist published as a shim that re-exports from vlist.

  ci: add npm publish workflow with Trusted Publishers (OIDC)

2026-04-15  v1.5.4

  feat(selection): implement ARIA multi-select keyboard model (#27)
    Rewrites the multi-select keyboard interaction to follow the WAI-ARIA
    APG recommended listbox pattern, replacing the anchor-based range model:
    - Shift+Arrow toggles one item at a time (additive, not range-based)
    - Shift+Space selects contiguous range from last selected to focused
    - Ctrl+Shift+Home/End selects from focused item to first/last item
    - Ctrl+A / Cmd+A selects all (or deselects all if all selected)
    - Shift+Click range selection uses lastSelectedIndex as anchor
    New shiftArrowToggle config option:
    - 'origin' (default) — toggles the item being left (macOS-style)
    - 'destination' — toggles the item being moved to (ARIA literal)
    Both modes are additive — prior selections are always preserved.
    Closes #27.

2026-04-14  v1.5.3

  feat(selection): add Shift+keyboard range selection in multiple mode
    Holding Shift while pressing Arrow/Page/Home/End keys now selects a
    contiguous range from an anchor point to the new focus position:
    - Shift+Arrow extends or shrinks the range by one item (or row/lane)
    - Shift+Page Up/Down extends the range by one page
    - Shift+Home/End extends the range to the first/last item
    - Anchor is set on non-shift navigation, Space/Enter, or click
    - Shift+click also uses the shared anchor for consistency
    - No effect in single mode (guarded by mode === 'multiple')
    Works with flat lists, grids, and masonry layouts.
    Closes #24.

2026-04-13  v1.5.2

  feat(snapshots): add autoSave for automatic sessionStorage save/restore
    withSnapshots({ autoSave: 'key' }) handles everything automatically:
    - Saves to sessionStorage on scroll:idle and selection:change
    - Restores scroll position + selection on next build()
    - Cancels withAsync's autoLoad and bootstraps total from snapshot
    - Event-based restore guard (lifts on first scroll:idle, no timers)
    Eliminates all manual snapshot plumbing — no event listeners,
    debounce timers, or sessionStorage calls needed in user code.

  feat(core): rename accessible config to interactive
    Rename `accessible` → `interactive` for clarity — `accessible: false`
    sounds like it hurts accessibility when it really just disables
    item-level keyboard focus. The old name is kept as a deprecated alias.

  feat(async): defer autoLoad to queueMicrotask (cancellable)
    autoLoad is now deferred via queueMicrotask so that higher-priority
    features (e.g. withSnapshots) can cancel it during their own setup.
    Exposes _cancelAutoLoad on ctx.methods for inter-feature coordination.

  fix(snapshots): compressed-mode save/restore roundtrip drift
    Use compression.virtualSize consistently for both save and restore.
    viewportState.totalSize includes compression slack which differs
    between runtime and restore bootstrap, causing cumulative ~224px
    drift per roundtrip at scale.

  docs(core): add Base Configuration section to README
    Document all base vlist() options (interactive, overscan, padding,
    orientation, scroll.*) — previously undiscoverable.

2026-04-13  v1.5.1

  fix(autoSize): scroll-to-bottom not reaching the end with auto-sized items
    Three issues prevented scrollToIndex(last, 'end') from reaching the true
    bottom when using withAutoSize():
    - _setSizeCache gap-subtraction stacking: each ResizeObserver batch re-wrapped
      getTotalSize() with another gap subtraction, making content height shrink by
      N×gap pixels. Fixed with a WeakSet guard for idempotent wrapping and by
      removing the redundant setSizeCache call from the RO callback.
    - Content size deferred at bottom: now checks isAtBottom and updates immediately
      when at the bottom, with a forced layout reflow before snapping scroll position.
    - Stale smooth-scroll target in flush: two-tier at-bottom detection using DOM
      maxScroll and size-drift tolerance when the render range includes the last item.

  fix(autoSize): smooth scroll to bottom without visible correction jump
    - Dynamic animation target: animateScroll recalculates the target each frame
      from the current size cache so the destination adapts as measurements arrive.
    - Skip content-size deferral near the end: when the render range includes the
      last item, content size is always updated immediately so the browser does not
      clamp scrollTop during the animation.
    - Snap on measurement near end: when nearEnd is true and scrollTop was at the
      DOM maxScroll before the update, snap to the new bottom immediately.

  fix(selection): preserve selection across async loading
    Selection state is now transferred from placeholder items to their loaded
    replacements during async data fetching.

  fix(scale): keyboard navigation broken in compressed scroll mode
    Fixed scroll position calculation for keyboard navigation when withScale
    compression is active.

  feat(table): Left/Right arrow keys scroll horizontally by column width
    Arrow keys now scroll the table viewport horizontally by one column width,
    improving keyboard navigation in wide tables.

  fix(a11y): Home/End navigate to first/last item in grid mode
    Home and End keys now correctly navigate to the first and last items in
    grid layouts instead of being handled as row-level navigation.

  perf(groups,table): optimize feature bundle sizes
    withGroups reduced by 1.9 KB gzipped through code deduplication and
    simplified group header management.


2026-04-12  v1.5.0

  BREAKING: estimatedHeight/estimatedWidth now requires .use(withAutoSize())
    Auto-measurement (Mode B) is no longer built into the base bundle. Users who
    use estimatedHeight or estimatedWidth must add .use(withAutoSize()) to their
    builder chain. Framework adapters (vlist-react, vlist-vue, vlist-svelte,
    vlist-solidjs) auto-detect this and add the feature automatically — no changes
    needed for adapter users.

  feat(autosize): new withAutoSize() tree-shakeable feature (+0.7 KB gzip)
    Extracted Mode B auto-measurement into a standalone feature plugin. Items are
    rendered at the estimated size, measured via ResizeObserver, and pinned to their
    real size. Includes scroll correction, content size deferral during scrolling,
    and per-item observation via afterRenderBatch hook.

  perf(core): reduce base bundle from 11.3 KB to 10.3 KB gzipped
    - Extract ARIA keyboard navigation into builder/a11y.ts
    - Split scrollToFocus into simple (base) and compressed (features) variants
    - Simplify velocity tracker from circular buffer to 2-sample approach
    - Remove unused emitter methods (once, listenerCount)
    - Compact DOM structure creation with cssText and el() helper
    - Pre-compute CSS class name constants
    - Merge appendItems/prependItems reverse variants
    - Simplify API method resolution with helper
    - Inline constants (OVERSCAN, CLASS_PREFIX, SCROLL_IDLE_TIMEOUT)
    - Remove countVisibleItems from base (use indexAtOffset instead)

  fix(build): use wrapper entry points to prevent Bun tree-shaking dist
    Bun's bundler strips re-export barrel files into empty stubs. Work around
    this by generating wrapper entry points that force symbol inclusion.


2026-04-12  v1.4.5

  feat(styles): modular CSS — extract grid and masonry into dedicated stylesheets
    vlist-grid.css (1.2 KB) and vlist-masonry.css (1.3 KB) are now separate opt-in
    imports, matching the existing vlist-table.css pattern. Core CSS reduced from
    9.7 KB to 7.4 KB. New package exports: vlist/styles/grid, vlist/styles/masonry.
  feat(styles): redesigned item state colors using rgba for clear visual hierarchy
    Striped 2% → hover 4% → selected 12% blue → selected+hover 18% blue.
    Selected+hover now goes darker (was incorrectly lighter). Works on any background.
  feat(styles): restructured CSS custom properties
    Theme-invariant tokens (spacing, transitions, scrollbar geometry) separated from
    color tokens. All three dark mode blocks now contain identical color-only overrides.
    Added --vlist-bg-striped and --vlist-group-header-bg to all theme blocks.
  feat(styles): zebra striping with .vlist-item--odd compound selectors
    Selected state always overrides striped background via background shorthand
    and compound selectors (specificity 0,3,0).
  fix(styles): animation name mismatch — fade-in → vlist-fade-in
  fix(styles): table dark mode now supports all 3 patterns (data-theme-mode, prefers-color-scheme, .dark)
  fix(styles): stale fallback values in table CSS updated to match current token defaults
  fix(styles): loading overlay uses color-mix() for automatic dark mode adaptation
  fix(styles): .vlist-item--enter uses own vlist-item-enter keyframe (was conflicting with core)
  refactor(styles): remove broken .vlist--striped CSS-only approach (nth-child fails with DOM recycling)
  refactor(styles): remove dangerous .vlist--animate transform transition (caused scroll jitter)
  refactor(styles): remove redundant .vlist-scrollbar-hide (core has .vlist-viewport--no-scrollbar)
  refactor(styles): remove dead commented-out border-left code from selected state


2026-04-11  v1.4.4

  fix(selection): use scroll-if-needed on focusin instead of center-scroll
    onFocusIn always centered the focused item, causing a large gap below
    the last item when tabbing back after pressing End. Now uses scrollToFocus
    which only scrolls when the item is outside the viewport.
  fix(grid): scroll jump to bottom when clicking items in grid mode
    commitFocus passed flat item indices to scrollToFocus but the size cache
    is indexed by rows. Added i2s (item-to-scroll-index) mapping.
  fix(grid): baseline single-select keyboard nav for 2D grids
    ArrowUp/Down move by ±columns (row nav), ArrowLeft/Right by ±1 (cell nav).
    Home/End go to first/last cell in current row (WAI-ARIA Grid pattern).
    Ctrl+Home/End go to first/last item overall.
    PageUp/Down jump by visible rows while staying in column.
  fix(grid): wire setUpdateItemClassesFn to grid renderer
    withSelection focus/selection class updates were no-ops because the core
    rendered Map is empty in grid mode — must delegate to grid renderer.
  fix(grid): swap arrow keys for horizontal orientation
    Left/Right = ±columns (scroll axis), Up/Down = ±1 (cross axis).
  fix(masonry): lane-aware keyboard navigation
    ArrowUp/Down find prev/next item in same lane (visual column).
    ArrowLeft/Right find nearest item in adjacent lane by y-position.
    PageUp/Down jump by visible items in same lane.
    Horizontal orientation swaps axes correctly.
  fix(masonry): placement-based scroll-into-view for focus
    Core size cache has no meaningful per-item offsets in masonry mode.
    Registered _scrollItemIntoView using pre-calculated placement coordinates.
  fix(masonry): wire setUpdateItemClassesFn to masonry renderer
  fix(masonry): separate template re-evaluation from CSS class updates
    Clicking no longer flashes images — only CSS classes update on state change,
    preserving loaded <img> elements (matches grid renderer pattern).
  fix(selection): grid/masonry-aware keyboard navigation
    ArrowUp/Down pass delta=columns for grids, use _navigate for masonry.
    ArrowLeft/Right enabled for grid (±1) and masonry (adjacent lane).
    Home/End row-scoped in grid, Ctrl+Home/End grid-scoped.
    getPageSize uses i2s for correct row height lookup.
    scrollToFocus uses _scrollItemIntoView when available.
  fix(selection): moveFocusUp/moveFocusDown accept optional delta parameter
  fix(core): preventDefault for recognized nav keys at boundaries
  fix(groups): non-null assertion for groupLayout in renderInto (TS18047)
  perf(masonry): O(1)/O(log k) navigation with per-lane index arrays
    laneItems[lane] = sorted flat indices, itemLanePos[i] = position in lane,
    laneYCenters[lane] = Float64Array for binary search on adjacent-lane nav.
    ArrowUp/Down O(1), ArrowLeft/Right O(log k), PageUp/Down O(1).
    Index arrays rebuilt after each layout calculation.
  feat(photo-album): add withSelection to vanilla example
  test: 25 integration tests for grid/masonry navigation
    Vertical + horizontal orientation, baseline + withSelection,
    lane-aware masonry nav, renderer updateItemClasses preservation.

2026-04-10

  feat(groups): collapse first inline header when sticky is active (#8)
  feat(groups): sticky header architecture rewrite — recycled slots, pre-cached arrays (#8)
  feat(groups): template-driven sticky header via renderInto callback (#8)
  feat(groups): new header: { height, template } config shape (backwards compatible)
  feat(groups): orientation-aware header config (height/width)
  fix(sticky): variable-height header support in binary search (#8)
  fix(sticky): container size update in complete() for variable heights
  fix(sticky): slider sizing derived from actual header size, not hardcoded
  fix(css): remove redundant .vlist-sticky-group rule
  perf(sticky): pre-cache offsets/sizes in flat arrays — zero function calls per tick
  perf(sticky): module reduced from 413 to 230 lines (44% smaller)
  test(sticky): 100% coverage on sticky.ts
  test(groups): feature.ts coverage improved to 99.61%

  → v1.4.3


2026-04-10

  fix(scale): replace near-bottom interpolation with compression slack (#7)
  fix(scale): stale viewportState in early-return render path (#7)
  fix(scale): scrollbar bounds missing compression slack (#7)
  fix(scale): arrow-down from top scrolled unnecessarily (#7)
  fix(scale): fractional bottom-edge alignment in scrollToFocus (#7)
  fix(scale): bottom gap eliminated with exact slack formula (#7)
  fix(scale): CSS padding accounted for in compression slack (#7)
  fix(css): remove container :focus-visible outline per WAI-ARIA (#9)
  test(scale): 47 new compressed-scroll tests
  docs: update scale.md for compression slack approach

  → v1.4.2


2026-04-09

  test(core): close coverage gaps in measurement, api, and scale
  refactor(scale): remove redundant destroy cleanup, add clamping tests
  fix(selection): smart keyboard scroll + PageUp/PageDown support (#3)
  fix(selection): keyboard scroll in compressed mode (scale)
  fix(table): keyboard focus/selection classes not applied in table mode
  fix(css): hide container focus outline when a row is selected
  test(selection): add keyboard scroll, PageUp/PageDown, and table delegation tests

  → v1.4.1


2026-04-06

  fix(table): add compressed positioning support for withScale compatibility
  fix(table): add updatePositions() for efficient scroll-only position updates in compressed mode
  fix(scale): normalize scroll sensitivity for compressed lists
  test(table): add 5 compressed positioning tests (large dataset, force mode, updatePositions)

  → v1.4.0


2026-04-03

  feat(core): contextual error reporting — error event fires for template failures, feature setup errors, and destroy handler errors with ErrorViewportSnapshot


2026-03-30

  fix(api): re-apply template on updateItem for all renderers
  fix(grid): add placeholder class support to grid renderer


2026-03-24

  feat(selection): add selectNext and selectPrevious methods
  feat(core): add interactive config to disable keyboard navigation
  perf(builder): base bundle 27.0 KB minified (10.1 KB gzipped)

  → v1.3.9


2026-03-20

  fix(test): type restoreScrollMock parameter to fix TS2493 tuple error
  fix(scale): propagate clamped scroll position through scrollTo pipeline


2026-03-19

  feat(async): add reload({ snapshot }) for automatic scroll restore
  feat(async): add skipInitialLoad option to reload for snapshot restore optimization
  fix(scale): complete force compression lifecycle — native scroll sync, grid state, drift guard


2026-03-18

  fix(scale): use exact offset math when ratio=1 to eliminate bottom gap
  feat(scale): add force option to enable compressed mode on any list size


  → v1.3.8


2026-03-14

  fix(render): clear stale DOM nodes when last item removed — empty range returns end:-1
  feat(utils): export createStats and types from public API
  feat(api): expose getItemAt and getIndexById on public VList API


2026-03-12

  fix(test): apply CI_MULTIPLIER to all timing thresholds in performance tests
  fix(ci): pin setup-bun to v2.1.3 to avoid transient 401 errors


  feat(builder): dev-mode diagnostics — warn on non-array setItems, missing/duplicate IDs, out-of-range scrollToIndex, missing updateItem/removeItem targets, empty template return (DCE-verified, 0 KB prod overhead)
  fix(rendering): setItems() calls cancelScroll() before updating data
  fix(rendering): skip render when containerSize <= 0 (zero-height guard)
  feat(a11y): ARIA live region (aria-live=polite) for visible range announcements
  feat(a11y): focus recovery on removeItem() — nearest remaining item receives focus
  feat(grid): add conflicts declaration (masonry, table)
  feat(masonry): add conflicts declaration (grid, table)
  feat(styles): CSS :root defaults replace [data-theme-mode] selector
  feat(ci): tree-shaking verification (11 scenarios) integrated into measure-size.ts
  feat(ci): coverage threshold enforcement script (85% minimum)
  test: shared test helpers in test/helpers/ (dom, factory, timers)
  test: DOM snapshot tests — 11 tests across 6 configurations (base, grid, groups, masonry, table)
  test: 2,822 tests / 37,978 assertions, all passing
  perf(builder): base bundle 26.2 KB minified (9.8 KB gzipped)

  V1 code review — all 14 items complete ✅

  → v1.3.7


2026-03-11

  fix(async): removeItem — shift sparse storage, force render, debounced gap refill
  fix(async): convert row-space renderRange to item indices when grid is active
  fix(async): use live render range in loadPendingRange instead of stale saved coordinates
  fix(types): widen removeItem signature to string | number across all data managers
  feat(table): add placeholder skeleton styling for table rows
  perf(builder): base bundle 98.3 KB minified (32.5 KB gzipped)

  → v1.3.6


2026-03-09

  fix(table): account for cross-axis padding in column width resolution
  fix(async): eagerly analyze placeholder structure on first setItems with adapter
  perf(builder): base bundle 96.7 KB minified (31.9 KB gzipped)

  → v1.3.5

  feat(styles): add consolidated dark mode support with prefers-color-scheme, .dark class, and color-scheme
  feat(striped): add "data", "even", "odd" modes for group-aware striping
  feat(export): clean up
  perf(builder): base bundle 96.6 KB minified (31.8 KB gzipped)

  → v1.3.4


2026-03-08

  feat(item): add gap property for consistent spacing between items
  feat(config): add padding property for content inset around list boundaries
  refactor(range): move padding-aware scroll logic into calcScrollToPosition
  perf(builder): base bundle 24.7 KB minified (9.3 KB gzipped)

  → v1.3.3

  refactor(builder): extract measurement and api modules from core.ts
  refactor(builder): deduplicate idle handler, click/dblclick, stayAtEnd
  fix(scroll): wheel handler now fires afterScroll hooks and velocity:change events
  fix(scroll): prevent double-firing of scroll events during wheel scrolling
  perf(builder): base bundle −0.5 KB minified (23.6 KB, 8.8 KB gzipped)

  → v1.3.2


2026-03-07

  fix(grid): remove visibleEnd++ phantom row, use exclusive viewport boundary
  fix(grid): reduce RELEASE_GRACE from 2 to 1 to halve lingering DOM elements during scroll
  fix(grid): import OVERSCAN/CLASS_PREFIX/SCROLL_IDLE_TIMEOUT from shared constants
  fix(table): remove visibleEnd++ phantom row, use exclusive viewport boundary
  fix(table): remove RELEASE_GRACE entirely — table rows carry N cells, no grace needed
  fix(masonry): reduce RELEASE_GRACE from 2 to 1
  fix(masonry): extract overscan pixel multiplier into OVERSCAN constant
  refactor: remove redundant ?? 3 overscan fallbacks in grid, table, masonry features

  → v1.3.1


2026-03-06

  fix(core): replace UA sniffing with pointer:coarse capability detection
  fix(table): add missing TestItem generic to withTable call in sort test
  docs(contributing): rewrite to match v1.x builder/feature architecture
  chore(ci): add GitHub Actions workflow (typecheck, test, build, bundle size)
  test: improve coverage to 97% functions / 99.13% lines (+450 tests, 2719 total)

  → v1.3.0


2026-03-06

  fix(table): resolve ARIA required-children violations (role=presentation on header scroll container)
  fix(table): remove tabindex from viewport so role=none is not ignored by browsers
  fix(table): add aria-rowcount on grid, updated dynamically in render loop
  fix(selection): move live region outside role=grid (only row/rowgroup children allowed)
  feat(table): add striped row style with dedicated --vlist-bg-striped variable
  test(table): add 14 striped tests for table renderer, update ARIA tests

  → v1.2.9


2026-03-05

  feat(table): add withGroups compatibility for grouped data tables
  feat(table): group header row rendering (full-width, no cells, custom template)
  feat(table): expose _replaceTableRenderer, _updateTableForGroups, _getTableHeaderHeight hooks
  feat(groups): table-aware code path alongside existing grid path
  feat(groups): stickyOffset param for positioning below table column header
  style(table): .vlist-table-group-header styles, z-index layering, dark mode
  perf(table): sizeCache getter instead of captured reference (fixes stale ref after groups rebuild)
  perf(table): TrackedRow.isGroupHeader flag (replaces classList.contains on hot path)
  perf(table): lastHeight tracking, hoisted getSizeCache, removed per-frame width writes
  test(table): 22 new tests for group header rendering, type transitions, change tracking

  → v1.2.8


2026-03-04

  feat(a11y): reorder DOM children on scroll idle for screen readers
  feat(builder): add idleHandlers to BuilderContext for deferred scroll-idle work
  refactor(rendering): extract shared sortRenderedDOM utility for all renderers

  → v1.2.7


2026-03-04

  fix(grid,masonry): implement smooth scrollToIndex using createSmoothScroll
  fix(grid,masonry): cancel scroll animation on destroy to prevent rAF on torn-down DOM
  test(masonry): update scrollToIndex test for options object API, add rAF/cAF mocks

  → v1.2.6


2026-03-02

  fix(scroll): skip core wheel handler when compression is active (withScale owns scroll)

  → v1.2.5


2026-03-02

  fix(async): add deceleration debounce to prevent excessive API requests during momentum scroll
  fix(scroll): use actual DOM scroll limit in wheel handler instead of logical total size
  fix(scroll): reset velocity to zero when wheel events hit scroll boundary

  → v1.2.4


2026-03-02

  fix(scroll): allow horizontal scrolling in wheel handler for tables with wide columns
  style(css): swap selected/selected-hover colors to correct visual hierarchy
  style(css): remove default border-bottom from list items
  style(table): remove last-child border-right override on table column borders
  test(scroll): add 6 tests for horizontal scroll wheel passthrough

  feat(item): add striped option for reliable zebra-stripe styling
  fix(a11y): move role=listbox from root to items container

  → v1.2.3


2026-03-01

  fix(table): center resize handle on column border with pseudo-element

  → v1.2.2


2026-03-01

  feat(table): add withTable feature — virtualized data table with columns, resize, sort
  fix(builder): skip only undefined items in render loop, not falsy values
  fix(table): reset inherited vlist-item border-bottom on table rows
  refactor(table): move inline styles to dedicated CSS file
  refactor(table): micro-optimizations for runtime performance

  → v1.2.1


2026-03-01

  feat(a11y): keyboard focus, focus-visible, and baseline navigation
  feat(selection): scope hover highlight and pointer cursor behind --selectable class
  feat(grid,masonry): responsive resize with context injection
  fix(a11y): clear focus ring on blur and remove viewport from tab order
  fix(grid): remove trailing gap at bottom of grid
  fix(core): defer initial size cache build when grid/masonry will replace it
  refactor: rename withSections to withGroups

  → v1.2.0


2026-02-27

  BREAKING: BuiltVList type removed — use VList instead

  feat(types): add VListConfig type for framework adapters
  refactor(types): rename BuiltVList to VList, remove legacy types
  refactor: rename MAX_VIRTUAL_HEIGHT → MAX_VIRTUAL_SIZE, GridHeightContext → GridSizeContext
  docs(readme): update feature sizes, fix API method names, add size script

  → v1.1.0, v1.1.2


2026-02-26

  fix(builder): revert core inlined renderer optimizations causing positioning bug
  feat(masonry): add masonry layout feature with shortest-lane placement
  perf(masonry): optimize scroll hot path and fix boundary thrashing
  perf(grid): apply feature optimization playbook
  perf(renderer): apply patterns 9-11 — grace period, change tracking, lazy fragment
  perf(builder): apply patterns 9-11 to core inlined renderer
  perf(selection): feed selection state through render pipeline instead of DOM bypass
  refactor(masonry): reduce bundle weight — flatten types, remove dead code
  refactor(grid): remove tick() — always call render(), rely on change tracking

  → v1.0.0, v1.0.1


2026-02-25

  fix(scale): correct scrollbar detection to prevent duplicates
  test(selection): add missing getCached method to dataManager mock


2026-02-24

  feat(placeholder): per-item length profiles from first batch, CSS-driven skeleton
  feat(rendering): add Mode B auto-measurement with MeasuredSizeCache
  feat(async): reload visible placeholders on network recovery
  perf(selection): avoid O(total) rebuildIdIndex with async data
  fix(grid): swap CSS width/height in horizontal mode for correct aspect ratio
  fix(grid): use cross-axis container dimension for horizontal grid sizing
  fix(grid): rebuild size cache on resize for dynamic height functions
  fix(core): use main-axis dimension for containerSize in horizontal mode
  test: add Phase 1 + Phase 2 integration, memory, and performance tests (+184 tests)
  test(builder): add comprehensive SimpleDataManager tests

  → v0.9.6, v0.9.7, v0.9.8, v0.9.9


2026-02-23

  feat(async): add autoLoad option to defer initial data loading
  feat(async): add storage config option for chunk size control
  feat(snapshots): add restore config, total in snapshot, NaN guard, tests
  fix(async): prevent duplicate requests on reload and optimize initial load
  fix(async): restore placeholder replacement in onItemsLoaded and reload
  fix(snapshots): restore scroll position with async autoLoad:false

  → v0.9.5


2026-02-22

  feat(scroll): implement scroll.wheel and scroll.scrollbar config options
  refactor: complete plugin → feature terminology migration
  fix(page): prevent scroll conflicts in window scroll mode
  fix(scale): prevent scroll beyond bottom + rename variables for horizontal support

  → v0.9.2, v0.9.3, v0.9.4


2026-02-21

  BREAKING: direction → orientation for semantic clarity
  BREAKING: HeightCache → SizeCache — all height-specific APIs renamed
  BREAKING: scrollTop → scrollPosition in ViewportState and events

  feat(sections): add horizontal mode support
  feat(grid): allow horizontal orientation
  refactor(core): add SizeCache implementation and tests
  refactor(core): update entire codebase to use SizeCache
  refactor(breaking): remove HeightCache backward compatibility
  refactor(cleanup): systematically rename all height references to size
  refactor(viewport): update ViewportState and CompressionState to use size terminology
  refactor: rename plugin to feature for consistency
  fix(types): complete dimension-agnostic refactoring — all tests passing
  fix(events): complete scroll event API migration to scrollPosition
  fix(grid): swap axes for horizontal orientation positioning
  test: enhance coverage to 97.54% with comprehensive tests

  → v0.9.0, v0.9.1


2026-02-20

  fix(core): add wheel interception for Chrome/Safari to prevent scroll race condition
  fix(core): disable wheel interception on mobile devices
  fix(selection): restore O(1) ID lookups to prevent selectAll freeze
  refactor(core): remove browser detection from wheel handler — apply to all browsers

  → v0.8.1, v0.8.2


2026-02-19

  feat(builder): add memory optimization config flags (then make default)
  fix(scale): smooth scroll interpolation to fix Firefox compressed scroll-up bug
  fix(scale): add touch scroll support for compressed mode on iOS/mobile
  fix(async): reload now clears DOM, shows placeholders, and loads only visible range
  fix(async): prevent endless loadMore cascade at bottom of sparse lists
  fix(builder): add missing invalidateRendered stub to simplified context
  fix(builder): remove array copying from setItems()
  fix(range): use accurate visible item counting to prevent blank areas during fast scroll
  refactor(builder): decompose core.ts with refs-object pattern
  refactor(builder): use short keys on refs object to reduce bundle size
  refactor: remove deprecated core files, simplify build

  → v0.7.4, v0.7.5, v0.7.6, v0.7.7, v0.8.0


2026-02-18

  feat: move framework adapters to separate packages (vlist-react, vlist-vue, vlist-svelte)
  feat: remove createVList from public API — builder-only entry point
  feat(animation): add placeholder pulse and smooth fade-in on replacement
  refactor(core): move full.ts to vlist.ts as main entry point

  → v0.7.0, v0.7.1, v0.7.2, v0.7.3


2026-02-17

  refactor: rename plugins/ to features/ and render/ to rendering/
  refactor: reorganize core files into core/ directory
  refactor: simplify exports — builder-only API
  fix(builder): show native scrollbars by default

  → v0.6.0, v0.6.1, v0.6.2


2026-02-16

  feat(core): add double-click support and grid+groups combination
  feat(grid): support full-width group headers in grid layout
  feat(grid): implement groups-aware grid layout algorithm
  feat(grid): full groups support with proper layout and positioning
  feat(groups): allow sticky headers with reverse mode
  feat(groups): implement smooth scrolling in scrollToIndex override
  fix(grid): correct total height calculation for grid+groups combination
  fix(types): fix TypeScript errors in builder and plugins (43→19→0 errors)
  fix(tests): fix spread operator bug destroying getters
  fix(package): correct TypeScript declaration paths for Bundlephobia
  fix(package): add default export conditions and reorder exports
  fix(build): smart line-breaking for esbuild-wasm compatibility
  fix(adapters): use relative imports in React, Vue, and Svelte adapters
  refactor(tests): reorganize test structure to match src/ organization

  → v0.5.1, v0.5.2


2026-02-15

  feat(plugin): extract window scroll mode to plugin
  feat: add ultra-lightweight core-light entry point (4.9 KB)
  fix(grid): update scroll height after dynamic column change
  refactor(types): fix circular dependencies
  refactor: organize all plugins under src/plugins/ folder
  perf(constants): reduce cancel load velocity threshold from 25 to 15 px/ms

  → v0.5.0


2026-02-14

  refactor(core): make builder pattern the default entry point
  refactor(builder): remove legacy monolithic implementation and fix horizontal/window modes
  feat(builder): add built-in velocity tracking with circular buffer


2026-02-13

  fix(grid): calculate visible/render range for scroll virtualization
  fix(builder): sync viewport state and scrollbar bounds with compression
  fix(compression): eliminate gap at bottom when scrolled to end
  fix(core,builder): update lastScrollTop before rendering in all scroll operations


2026-02-12

  feat(scroll): add hover zone for scrollbar discovery and remove will-change
  feat(builder): self-contained composable builder with plugin system


2026-02-11

  test: consolidate coverage tests and fix gaps to reach 99.94%
  refactor(data): remove dead concurrent dedup guard in loadRange


2026-02-10

  feat(adapters): add React, Vue, and Svelte framework adapters
  feat(reverse): add reverse mode for chat-style UIs
  feat(scroll): implement scroll config with wheel control and custom scrollbar
  feat(scroll): add scroll.wrap for circular navigation
  feat(scroll): add horizontal scrolling support
  feat(a11y): aria-setsize, aria-posinset, aria-activedescendant, aria-busy, live region
  test: improve coverage to 94.78% funcs / 95.86% lines (+262 tests)
  refactor: move sandbox and docs to vlist.io


2026-02-09

  feat(groups): add sticky headers / grouped lists
  feat(grid): add 2D grid layout mode
  feat(grid): apply gap between rows, use real photos, aspect-ratio height
  feat(render): add variable item height support (Mode A)
  feat(scroll): smooth scrollToIndex animation + shrink bundle with sub-module exports
  feat(scroll): add window/document scrolling support
  feat(scroll): configurable idle timeout
  feat(scroll): add scroll position save/restore
  perf(render): zero-allocation scroll hot path with in-place range mutation
  perf(scroll): RAF-throttle native scroll handler
  perf(css): add CSS containment and scroll transition suppression
  perf(speed): batch Date.now() in sparse storage and mutate focusedIndex in-place
  perf(render): targeted focus update and cheaper pool release
  perf(size): split CSS into core/extras, CSS-only positioning, re-export range functions
  perf(speed): use direct getters instead of getState() on hot paths
  perf: minify CSS, add gzip compression to dev server
  fix: add aria-label support for listbox accessibility
  fix(scroll): velocity-based load cancellation for scrollbar drag
  fix(handlers): gate loadMore with velocity check to prevent spurious requests
  fix(render): fix phantom item on empty list

  → v0.3.1


2026-02-05

  refactor(api): group itemHeight and template under item config
  refactor(sandbox): rename examples to sandbox with improved DX
  refactor: improve build, serve, and package.json


2026-02-04

  feat: performance optimizations and configurable preloading
  perf(selection): use O(k) lookup instead of O(n) filtering for getSelectedItems
  fix(styles): show focus outline only for keyboard navigation


2026-02-03

  feat(scroll): add velocity-based load cancellation
  feat(loading): add configurable chunk preloading based on scroll direction
  fix(scroll): load pending range immediately when velocity drops below threshold
  fix(scroll): fix velocity tracker reset and missing range calculation
  perf(render): add DocumentFragment batching and optimize attribute setting
  perf: add reusable ItemState, ResizeObserver, and circular buffer velocity tracker


2026-02-02 — initial commit

  initial commit
  feat(compression): add support for 1M+ items via manual wheel scrolling
  feat(scrollbar): add custom scrollbar for compressed mode
  refactor: restructure to domain-driven architecture
  refactor: extract context, handlers, and methods from vlist.ts
  fix: resolve typecheck errors and runtime ctx initialization issue
  fix(render): replace placeholders with real data when items load
  perf: optimize bundle size and add size toggle to million-items example
  perf(vlist): optimize rendering and scroll performance

  → v0.2.0
