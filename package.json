{
  "name": "vlist.dev",
  "version": "0.1.0",
  "description": "vlist.dev — documentation, sandbox, and API for the vlist library",
  "type": "module",
  "scripts": {
    "dev": "bun run build:sandbox && bun run build:bench && bun run --watch server.ts",
    "start": "bun run server.ts",
    "build:sandbox": "bun run sandbox/build.ts",
    "build:sandbox:watch": "bun run sandbox/build.ts --watch",
    "build:bench": "bun run benchmarks/build.ts",
    "build:bench:watch": "bun run benchmarks/build.ts --watch",
    "typecheck": "tsc --noEmit",
    "link:libs": "cd ~/Code/floor/vlist && bun link && cd ~/Code/mtrl && bun link && cd ~/Code/mtrl-addons && bun link && cd ~/Code/floor/vlist.dev && bun link vlist && bun link mtrl && bun link mtrl-addons"
  },
  "dependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "marked": "^17.0.1",
    "mtrl": "file:../../mtrl",
    "mtrl-addons": "file:../../mtrl-addons",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "vlist": "file:../vlist",
    "vue": "^3.5.28"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "sass": "^1.85.0",
    "typescript": "^5.8.0"
  }
}
